spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  cloud:
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
management:
  tracing:
    enabled: false
  endpoints:
    web:
      exposure:
        include: info,health,configprops
  info:
    env:
      enabled: true
