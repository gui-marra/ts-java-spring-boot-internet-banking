spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  cloud:
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
management:
  tracing:
    enabled: false
  endpoints:
    web:
      exposure:
        include: info,health,configprops
  info:
    env:
      enabled: true
